version: '3.9'

services:

  postgres:
    image: postgres:14.1
    environment:
      POSTGRES_USER: "test"
      POSTGRES_PASSWORD: "password"
      POSTGRES_DB: "test"
    ports:
      - "5432:5432"
